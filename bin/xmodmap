#!/bin/sh
REAL_XMODMAP=/usr/bin/xmodmap
SETXKBMAP=/usr/bin/setxkbmap
XMODMAPFILE=${HOME}/.Xmodmap
SLEEP_DURATION=15s
DISPLAY=${DISPLAY:-1}

[[ $1 == '--no-sleep'  ]] || sleep ${SLEEP_DURATION}
DISPLAY=$DISPLAY ${SETXKBMAP}
DISPLAY=$DISPLAY ${REAL_XMODMAP} "${XMODMAPFILE}"
